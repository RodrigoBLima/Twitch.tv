import React from 'react';
import { Text } from 'react-native';

import { Wrapper, Heading} from './styles';

const CoomingSoon: React.FC = () => {
  return (
    <Wrapper>
      <Heading>CoomingSoon</Heading>
    </Wrapper>
  );
};

export default CoomingSoon;
